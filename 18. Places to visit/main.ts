let favPlaces: string[]=["Swat", "Fiji", "Switzerland", "Rome","USA", "Canada"];
for (let i=0; i<favPlaces.length; i++){
    console.log(favPlaces[i]);
    }

let sortedArray=favPlaces.sort();
console.log (sortedArray);

for (let i=0; i<favPlaces.length; i++){
    console.log(favPlaces[i]);
    }
let reversedArray=sortedArray.reverse();
console.log(reversedArray);

for (let i=0; i<favPlaces.length; i++){
    console.log(favPlaces[i]);
    }