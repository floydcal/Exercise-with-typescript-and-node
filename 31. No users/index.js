let userName = ["Floyd", "Waqas", "Shumail"];
if (userName.length > 0) {
    for (let username of userName) {
        console.log(`Hello ${username}, thank you for logging in!`);
    }
    let adminUser = "Arif";
    console.log(`Welcome ${adminUser}, would you like a status report`);
}
else {
    console.log("We need to find some users!");
}
userName = [];
if (userName.length > 0) {
    for (let username of userName) {
        console.log(`Hello ${username}, thank you for logging in!`);
    }
    let adminUser = "Arif";
    console.log(`Welcome ${adminUser}, would you like a status report`);
}
else {
    console.log("We need to find some users!");
}
export {};
